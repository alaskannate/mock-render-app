{
  "name": "mock-render-app",
  "version": "1.0.0",
  "description": "A web app allowing users to programatically view multiplex and multiscale images from within a Jupyter Notebook",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathaniel Ewing",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2" 
  }
}
